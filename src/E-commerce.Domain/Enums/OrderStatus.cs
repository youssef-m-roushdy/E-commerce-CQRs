namespace E_commerce.Domain.Enums
{
    public enum OrderStatus
    {
        PENDING,
        PROCESSING,
        SHIPPED,
        DELIVERED,
        CANCELED,
        RETURNED,
        REFUNDED
    }
}